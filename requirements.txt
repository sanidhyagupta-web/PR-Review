# Core
sqlalchemy>=2.0
cryptography>=42.0

# PDF parsing (typed PDFs)
pymupdf>=1.24          # import as "fitz"

# OCR (scanned PDFs)
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.0
pandas>=2.0             # for tesseract confidence parsing

# Embeddings
sentence-transformers>=3.0

# Vector store
chromadb>=0.5

# Keyword search
rank-bm25>=0.2.2

# PII detection (optional; regex fallback used if missing)
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
# Also requires spacy model:
# python -m spacy download en_core_web_lg

# AWS S3 + KMS
boto3>=1.34

# Streamlit UI
streamlit>=1.35

# FastAPI (bulk ingest HTTP endpoint)
fastapi>=0.111
python-multipart>=0.0.9
uvicorn>=0.30

# LLM inference (ADE extraction server — Apple Silicon)
mlx-lm>=0.19          # 4-bit quantized inference on MPS, no CUDA needed
torch>=2.2            # needed for adapter conversion script only
transformers>=4.40    # needed for adapter conversion script only
peft>=0.10            # needed for adapter conversion script only
safetensors>=0.4      # needed for adapter conversion script only

# Dev / testing
pytest>=8.0
pytest-cov
